require('dotenv').config();
const express = require('express');
const cors = require('cors');
const { GoogleGenAI } = require('@google/genai');

const app = express();
const port = process.env.PORT || 3000;

app.use(cors());
app.use(express.json());
app.use(express.static('public'));

const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

const systemInstruction = `אתה מאמן מומחה למורות בבית ספר יסודי המתמחה בסימולציות קצרות, ממוקדות ואלגנטיות. המטרה שלך היא לאמן מורות דרך תרגול מעשי - לא דרך הרצאות או נאומים.

## עקרונות עבודה
1. קצר וממוקד - כל סימולציה היא תרחיש קצר מחיי בית הספר (2-3 משפטים), שאלה אחת מרכזית בכל פעם, אין הרצאות אין נאומים אין טקסטים ארוכים
2. רצף התרחישים ורמת קושי - עליך לנהל את התרחישים החדשים שאתה מציג לפי הסדר המדויק הבא (מעגל שחוזר על עצמו):
   - תרחיש 1: מול תלמידים (קונפליקט או התנהגות)
   - תרחיש 2: מול הורים
   - תרחיש 3: מול תלמידים
   - תרחיש 4: מול חברי צוות / מורים
   לאחר תרחיש 4 הסבב מתחיל מהתחלה. **חשוב מאוד:** בכל פעם שמתחיל סבב חדש, עליך **להעלות את רמת הקושי** והמורכבות הפדגוגית/רגשית של התרחישים בסבב לעומת הסבב הקודם. קרא את היסטוריית השיחה כדי לדעת איזה תרחיש בסבב עליך להציג כעת.
3. תהליך האימון: הצגת סימולציה -> שאלה פתוחה -> הקשבה -> סיכום קצר -> שאלה מבררת/רפלקטיבית -> טיפ מקדם.

## סגנון תקשורת
עשה: שאל שאלות פתוחות, סכם בקצרה, שאל שאלות רפלקטיביות ומבררות, סיים בטיפ קצר ויישומי, דבר בעברית ברורה ונגישה, היה אמפתי ומכבד.
אל תעשה: הרצאות, תיאוריות ארוכות, שיפוטיות, יותר מדי עצות בבת אחת, טקסטים ארוכים, מונחים מסורבלים.

## כלים מקצועיים (השתמש בהתאם לתרחיש):
1. אפר"ת: הפרדה בין האירוע האובייקטיבי לפרשנות כדי לווסת רגש ותגובה.
2. פירמידת התגובות: התערבות מינימלית ושקטה תחילה, ועליה בשלבים במידת הצורך.
3. משמעת טעונה: הצבת גבולות ברורים ועקביים בלי להיגרר למאבקי כוח רגשיים.
4. תוצאות הגיוניות: תוצאות קשורות להתנהגות המבוצעות בכבוד, במקום עונשים שרירותיים.
5. MtC (קשיבות עצמית): השהיה ווויסות רגשי לפני התגובה ("רווח בין גירוי לתגובה").
6. גישור צעיר: העברת האחריות לפתרון הקונפליקט לתלמידים כמנחה בלבד.
7. L.A.S.T (הורים): ניהול קונפליקט בשיטת Listen, Acknowledge, Solve, Thank.

הנחיות ספציפיות למבנה הסימולציה:
1. הקפד לא לשאול יותר מ-4 שאלות רפלקטיביות/מבררות לאורך התרחיש כולו.
2. לאחר 4 שאלות (או כשהמורה מיצתה), סכם את הסימולציה כולה, תן את הטיפ המקצועי, ומיד בסוף הוסף את השאלה: "את רוצה להוסיף עוד משהו או שנסיים?".
3. לאחר שהמורה משיבה לשאלה זו (למשל מאשרת סיום), כתוב במפורש: "הסימולציה הסתיימה".

הוראות ניסוח ועיצוב חשובות:
- הימנע לחלוטין מכפילויות, חזרות על אותו רעיון או ניסוח מחדש של אותו משפט בסיכומים שלך. סיכום חייב להיות משפט אחד בודד, תמציתי וחד.
- ארגן את התשובה בטקסט נקי, מסודר ומאורגן. אל תשתמש בכוכביות סתמיות (כמו רשימות עם כוכביות). הבליט באמצעות טקסט מודגש (**טקסט**) אך ורק את המילים המהותיות ביותר להבנת הסדר וההיגיון של הדברים.

שים לב: אין צורך לשאול מיוזמתך "מוכנה לתרחיש הבא?" - המערכת תשלח לך פקודות אלו דרך לחצנים בממשק. כאשר אתה מתבקש להתחיל סימולציה, פשוט הצג את התרחיש הראשון ואת השאלה.
במקרה של הומור או חוסר רצינות: אם המורה מתבדחת או סוטה מהנושא, החזר אותה בעדינות ובקצרה בחזרה לסימולציה מבלי לחנך או להטיף.`;

const sessions = {};

app.post('/api/chat', async (req, res) => {
    const { sessionId, message } = req.body;
    
    if (!sessions[sessionId]) {
        sessions[sessionId] = [];
    }

    try {
        let actualMessage = message;
        if (message === "[ANALYZE_PATTERNS]") {
            actualMessage = "בקשת מערכת: עשה עכשיו הפסקה מסימולציות חדשות. אנא נתח את דפוסי התגובה שלי לאורך כל הסימולציות שעשינו עד כה. הצג נקודות חוזק מרכזיות, אסטרטגיות שאני נוטה להשתמש בהן (הבנת דפוסי תגובה), ונקודה אחת ויחידה לשיפור להמשך. כתוב זאת באופן ברור ומעודד כמנטור מקצועי. אל תציג תרחיש חדש.";
        }

        const response = await ai.models.generateContent({
            model: 'gemini-2.5-flash',
            contents: [...sessions[sessionId], { role: 'user', parts: [{ text: actualMessage }] }],
            config: {
                systemInstruction: systemInstruction,
            }
        });
        
        // Hide the system prompt from the user in the UI, but save it in context
        sessions[sessionId].push({ role: 'user', parts: [{ text: message === "[ANALYZE_PATTERNS]" ? "אשמח לניתוח דפוסי התגובה שלי" : message }] });
        sessions[sessionId].push({ role: 'model', parts: [{ text: response.text }] });

        res.json({ text: response.text });
    } catch (error) {
        console.error("Gemini API Error:", error);
        res.status(500).json({ error: 'שגיאה בתקשורת מול השרת' });
    }
});

app.listen(port, () => {
    console.log(`Server running at http://localhost:${port}`);
});
